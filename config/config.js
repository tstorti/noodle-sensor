exports.settings = {
    deviceID: 4,
    //TODO - if we want to customize code to allow for presence of individual sensors/devices
    // tempHumidity: true,
    // soilMoisture: true,
    // lightSensor: true,
    // waterPump: true,
    // lamp:true,
  };

